import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'io.ionic.logistics.app',
	appName: 'LogisticsApp',
	webDir: 'www',
	bundledWebRuntime: false
};

export default config;
